import SwiftUI
import FormbricksSDK

struct ContentView: View {
    var body: some View {
        HStack {
            VStack {
                Text("SAS")
                Spacer()
            }
            .padding()
            Spacer()
        }
        .background(Color.blue)
    
    }
    
}

#Preview {
    ContentView()
}
